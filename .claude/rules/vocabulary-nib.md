# Nib (not tip)

**Canonical:** `context/LEXICON.md` — **Nib** seated `20260712.121719`.

In new prose, rules, session logs, TASKS/RTAC living lines, and Bron fields, use **nib** for the landed edge of the work:

| Form | Meaning |
|------|---------|
| **product nib** | Civic/product parity pin (e.g. **430**) |
| **suite nib** | Witness-suite parity pin (e.g. **432**) |
| **git nib** | HEAD commit on the living branch (e.g. `bf05fbf`) |

Do **not** invent a new *tip* in forward-facing text. Historical *tip* in dated logs, counsel titles, and frozen witnesses stays readable (accrete-never-break).

Bron session logs prefer `product_nib` · `suite_nib` · `git_nib` (or `nib <hash>`) over `tip` / `product_tip`.
